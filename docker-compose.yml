version: "3"
services:
  app:
    container_name: booking
    image: booking
    restart: always
    build: .
    stdin_open: true
    tty: true
    ports:
      - "80:3002"
    volumes:
      - .:/app-booking
    links:
      - mongo

  mongo:
    image: mongo
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - ./data/db




